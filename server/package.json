{
	"name": "it-academy-node",
	"version": "1.0.0",
	"description": "",
	"main": "app.ts",
	"scripts": {
		"build": "rm -R dist && tsc -p tsconfig.json",
		"pm2": "pm2 start dist/app.js --watch -i max",
		"dev": "concurrently \"npx tsc --watch\" \"nodemon -q dist/app.js\""
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@types/cors": "^2.8.13",
		"cors": "^2.8.5",
		"dotenv": "^16.3.1",
		"express": "^4.18.2"
	},
	"devDependencies": {
		"@types/express": "^4.17.17",
		"@types/node": "^20.3.1",
		"concurrently": "^8.2.0",
		"nodemon": "^2.0.22",
		"typescript": "^5.1.3"
	}
}
